## 🎛 修图总控面板 + 连续多关透传（v1.0.5）

- **新增 🎛 HermesRetouchPanel 修图总控面板**：功能1-7 × [开关/提示词/强度] 集中控制。
  - 开关 关 → 对应功能整模块（提示词/采样/解码/预览）跳过，省算力
  - 提示词 → 0.8s 同步到对应节点（画布实时可见）
  - 强度 → 同步到对应采样器 denoise
  - 面板为唯一编辑入口：直接改子节点会被面板值覆盖
- **连续多关逐级透传（链脊手术）**：LiteGraph mode=4 自动重连只解一层不递归，
  JS 主动把"下一个开启模块输入→上一个开启模块解码输出"，被关模块从执行剔除。
  实测：连关1-5→功能6输入直连预处理；全关7个→原图直出（9节点）。
- **面板定位不写死节点 id**：按标题「功能N·…」+连线拓扑发现，新模块标题带前缀即自动纳入

## 打包（开箱即用）
- zip 含：ComfyUI 插件（Python 节点 + JS + 7份文档）+ Hermes 侧 MCP server + INSTALL/README + 测试画布
- 全流程：ComfyUI 侧 Zero-dependencies 解压入 custom_nodes；Hermes 侧按 README 三步注册
- 测试画布 `examples/qianwen修改图2.json`（含总控面板）= 使用本插件开发，兼安装验收

## 实测验收记录
- 连续关1-5：prompt 16节点，功能6输入=预处理，被关20节点全剔除
- 全关7：prompt 9节点，原图直通保存
- 全开：七功能链脊全部正确（每个功能输入=上一功能解码输出）
